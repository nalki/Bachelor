# 네트워크 통신 과정

---

# 1. DNS 조회 프로세스

> 클라이언트가 브라우저 주소창에 `www.google.com`을 입력하면 시스템은 이 문자를 컴퓨터가 이해할 수 있는 IP 주소로 변환하기 위해 DNS 서비스를 이용합니다.

## 1.1 Cache 확인 및 DNS 서버 접속

```text
Browser Cache: '내부 IP 주소 확인'
Client → DNS: '서버 접속'
```

- 호스트 내부 Cache의 기록을 확인하여 기록이 없다면 DNS 서버와 통신을 시작합니다. 
- DNS는 오버헤드가 적고 빠른 응답이 가능한 **UDP 프로토콜(포트 53)** 를 사용합니다.

## 1.2 DNS 질의 수행

### 1.2.1 재귀적 질의

```text
Client → DNS: '최종 IP 주소 요청'
```

- 클라이언트는 DNS 서버에게 **최종 IP 주소**를 요청합니다.

### 1.2.2 반복적 질의

```text
① DNS → Root DNS: '.com 네임 서버 주소 반환'
② DNS → TLD DNS: 'google.com 네임 서버 주소 반환'
③ DNS → Authoritative DNS: 'www.google.com의 최종 IP 주소 반환'
```

- DNS 서버가 전 세계 계층화된 **하위 DNS 서버**를 하나씩 접속해 정보를 수집하는 과정입니다. 
- 각 서버는 자신이 아는 범위 내에서 다음 목적지를 알려줍니다.

## 1.3 IP 주소 반환

```text
DNS → Client: '최종 IP 주소 반환'
```

- 모든 질의를 마친 DNS 서버가 **최종 IP 주소**를 클라이언트에게 반환합니다.

# 2. TCP 연결

> 데이터의 안전한 전송을 위해 `TCP 프로토콜(포트 80)`을 사용하여 서버와 신뢰성 있는 연결을 맺습니다.

## 2.1 세션 수립(3-Way-Handshake)

```text
① Client → Server: 'SYN(연결 요청)'
② Server → Client: 'SYN-ACK(연결 요청 확인 및 응답)'
③ Client → Server: 'ACK(연결 확립 완료)'
```

- **HTTP 프로토콜(포트 80)** 메시지 요청을 위해 3번의 패킷 교환을 통해 두 장치 간의 논리적인 통신 통로가 완전히 개방됩니다.

# 3. HTTP 요청 메시지 

> `HTTP 요청 메시지`는 응용 계층에서 작성되며 앞서 연결된 TCP 연결을 통해 클라이언트가 원하는 웹 페이지를 요청하는 단계입니다.

## 3.1 HTTP 요청 메시지 생성

```http
GET / HTTP/1.1
Host: [www.google.com](https://www.google.com)
User-Agent: Mozilla/5.0 (Windows NT 10.0; Win64; x64) ...
Accept: text/html,application/xhtml+xml ...
Connection: keep-alive
```

- 브라우저는 구글 서버에게 메인 화면 데이터를 요청하는 **GET 메시지**를 생성합니다. 
- 생성된 HTTP 요청 메시지는 크게 세 부분 **(시작 줄, 헤더, 바디)** 으로 나누어집니다.

## 3.2 HTTP 요청 메시지 구조

### 3.2.1 시작 줄

```text
GET / HTTP/1.1
메서드(GET) + 요청 경로(/) + 프로토콜 버전(HTTP/1.1)
```

- 시작 줄은 서버에 **무엇을(메서드)**, **어디서(요청 경로)**, **어떤 방식으로(프로토콜 버전)** 처리해달라고 명시하는 첫 번째 줄입니다.

### 3.2.2 헤더

```text
Host: '요청이 전송될 서버의 도메인 주소'
User-Agent: '요청을 보내는 브라우저 및 OS의 정보'
Accept: '브라우저가 이해할 수 있는 콘텐츠 타입(HTML, XML 등)'
Connection: '통신 연결 유지 여부(keep-alive)'
```

- 헤더는 메시지 자체에 대한 **부가 정보**를 담고 있습니다. 
- 서버는 이 헤더 정보를 보고 브라우저의 환경에 **최적화된 응답**을 준비합니다.

### 3.2.3 바디

```text
GET 요청의 경우 보통 비어 있음
```
- 바디는 서버로 보낼 **실제 데이터**가 담기는 부분입니다.
- POST 방식의 경우 로그인 정보나 게시글 내용이 담깁니다.

### 3.2.4 메소드 

```text
GET(조회): '서버로부터 특정 리소스를 요청'
POST(생성): '서버에 새로운 리소스를 생성'
PUT(수정): '서버의 리소스를 요청 메시지의 내용으로 대체'
PATCH(부분 수정): '서버 리소스의 일부분을 수정'
DELETE(삭제): '서버의 특정 리소스를 삭제'
```

- HTTP 메소드는 클라이언트가 서버에게 **요청의 목적**을 알리는 수단입니다.

# 4. 전송 계층

> 응용 계층에서 완성된 HTTP 요청 메시지가 하위 계층인 전송 계층으로 전달되어 데이터의 `전송 방식`과 `목적지 프로세스`를 결정하는 단계입니다.

## 4.1 세그먼트 생성

```text
HTTP 요청 메시지 + TCP Header → TCP Segment
```

- 전송 계층은 전달받은 HTTP 메시지 데이터에 전송 제어 정보를 담은 **TCP 헤더**를 추가해 캡슐화하며 이 단위를 **세그먼트(Segment)** 라고 부릅니다.

## 4.2 TCP 헤더 구조

```text
Source Port: '호스트에서 임의로 할당한 포트 번호'
Destination Port: '목적지 서버의 서비스 포트 번호'
Sequence Number: '분할된 세그먼트의 순서를 나타내는 번호'
Acknowledgment Number: '데이터를 잘 받았음을 확인해주는 응답 번호'
Window Size: '한 번에 보낼 수 있는 데이터의 양'
Checksum: '데이터 전송 중 오류 발생 여부를 확인하기 위한 값'
```

- TCP 헤더의 **Destination Port** 필드로 인해 구글 서버 내 수많은 프로그램 중 **웹 서버(HTTP)** 를 찾아갈 수 있습니다.

# 5. 네트워크 계층

> 전송 계층에서 내려온 세그먼트에 논리적 주소인 IP 정보를 부여하여, 전 세계 네트워크 망에서 목적지를 찾아갈 수 있도록 `패킷화`하는 단계입니다.

## 5.1 패킷 생성

```text
TCP Segment + IP Header → IP Packet
```

- 네트워크 계층은 전송 계층에서 전달 받은 세그먼트를 페이로드로 삼고 **IP 헤더**를 추가해 캡슐화하며 이 데이터 단위를 **패킷(Packet)** 이라고 부릅니다.

## 5.2 IP 헤더 구조

```text
Version: 'IP 버전 정보'
Time To Live(TTL): '패킷이 네트워크상에 살아있을 수 있는 시간'
Protocol: '상위 계층 프로토콜 타입'
Source IP Address: '송신자의 공인 IP 주소'
Destination IP Address: '수신자의 공인 IP 주소'
```

- 전송 계층의 포트 번호가 어떤 프로그램인지를 알려준다면 IP 주소는 어떤 집인지를 알려주는 **논리적 주소** 역할을 합니다.

## 5.3 라우팅 수행

- 구글 공인 IP 주소는 다른 서브넷이므로 라우팅 테이블에 설정된 기본 게이트웨이 규칙에 따라 **기본 게이트웨이 IP 주소**로 패킷을 전송합니다.

## 5.4 ARP 프로토콜 수행

### 5.4.1 ARP Cache 확인

- 호스트는 자신의 메모리에 있는 **ARP 테이블**을 확인합니다.

### 5.4.2 ARP Request

- 호스트의 ARP 테이블에 기본 게이트웨이의 MAC 주소가 없으면 자신의 페이로드에 기본 게이트웨이의 IP 주소를 담아 **브로드캐스트** 즉 네트워크 안의 모든 노드에 ARP 요청 패킷을 전송합니다.

### 5.4.3 ARP Reply

- ARP 요청 패킷을 받은 기본 게이트웨이는 자신의 페이로드에 MAC 주소를 담아 **유니캐스트** 즉 요청한 호스트에게만 패킷을 전송합니다.

### 5.4.4 ARP 테이블 갱신

- 호스트는 전송 받은 MAC 주소를 ARP 테이블에 **업데이트**하고 데이터를 하위 계층인 데이터링크 계층으로 전달합니다.

# 6. 데이터 링크 계층

> 네트워크 계층에서 내려온 패킷에 MAC 주소 정보를 추가하여 인접한 네트워크 장치 간의 신뢰성 있는 전송을 보장하는 `프레임(Frame)`을 생성하는 단계입니다.

## 6.1 프레임 생성

```text
Ethernet Header + IP Packet + Ethernet Trailer → Ethernet Frame
```

- 데이터 링크 계층은 패킷을 페이로드로 삼아 앞에는 **이더넷 헤더**, 뒤에는 오류 검출을 위한 **트레일러(Trailer)** 를 추가해 캡슐화하며 이 단위를 **프레임**이라고 부릅니다.

## 6.2 이더넷 헤더 구조

```text
Destination MAC Address: '다음 목적지의 물리적 주소'
Source MAC Address: '송신자의 물리적 주소'
EtherType: '상위 계층의 프로토콜 타입'
FCS(Frame Check Sequence): '데이터 에러 발생 여부를 위한 체크섬 값'
```

- 이더넷은 **인접한 노드** 즉 같은 스위치에 연결된 장치의 통신만을 책임집니다.
- MAC 주소는 **홉(hop)** 마다 계속 변경됩니다.

# 7. 물리 계층
> 데이터 링크 계층에서 완성된 프레임을 `물리적 신호`로 변환하여 전송하는 단계입니다.

## 7.1 비트 스트림의 신호 변환

```text
Digital Bits (0, 1) → Physical Signals
```

- **0** 과 **1** 로 이루어진 디지털 비트를 전기적 파동이나 빛의 신호로 변환하여 전송하는 역할을 수행합니다.

## 7.2 물리적 전송 매체 및 하드웨어

```text
NIC(Network Interface Card): '디지털 데이터를 물리적 신호로 변환하는 장치'
허브(Hub): '여러 장치를 연결하고 모든 포트로 신호를 전달하는 장치'
리피터(Repeater): '약해진 신호를 증폭하거나 재생하는 장치'
```

- 이 단계에서는 데이터의 내용을 볼 수 없고 오직 들어온 신호를 그대로 증폭하거나 다른 포트로 전달하는 **물리적 작업**만 수행합니다.

## 7.3 신호 전송

- 변환된 신호가 호스트의 랜 포트를 떠나 랜선을 타고 공유기나 벽면 단자함으로 전송됩니다.

# 8. AS 네트워크

> `AS(Autonomous System)`는 구글이나 ISP 같은 대형 사업자들이 운영하는 독립적인 네트워크들의 집합이며 이 AS들은 고유한 `ASN(Autonomous System Number)`을 부여받습니다.

## 8.1 NAT 수행

```text
Private IP → NAT → Public IP
```

- 기본 게이트웨이는 외부 인터넷 접속을 위해 패킷의 출발지 IP를 **공인 IP**로 변환하는 **NAT(Network Address Translation)** 를 수행합니다.

## 8.2 ISP 라우터

```text
Default Gateway → ISP: 'ISP 라우터 진입'
```
- **ISP(Internet Service Provider)** 는 개인이나 기업이 인터넷에 접속할 수 있도록 네트워크 연결 서비스를 제공하는 사업자입니다.
- 공인 IP로 변환된 패킷은 기본 게이트웨이에서 ISP 라우터로 전달됩니다.


## 8.3 광역 라우팅

### 8.3.1 IGP(Interior Gateway Protocol)

```text
Router A → IGP → Router B → IGP → Router C
```

- IGP는 AS 내부에서 사용되는 라우팅 프로토콜이며 대표적으로 아래와 같은 프로토콜이 있습니다.
  - **OSPF(Open Shortest Path First)**: 링크 상태 라우팅 프로토콜
  - **IS-IS(Intermediate System to Intermediate System)**: 링크 상태 라우팅 프로토콜
  - **RIP(Routing Information Protocol)**: 거리 벡터 라우팅 프로토콜

- AS 네트워크 내부의 상세한 토폴로지 정보를 바탕으로 **최단 거리**나 **낮은 지연 시간** 위주의 빠른 전송을 목표로 합니다.
  

### 8.3.2 EGP(Exterior Gateway Protocol)

```text
AS(ISP) ↔ BGP ↔ AS(Transit ISP) ↔ BGP ↔ AS(Google)
```

- EGP는 서로 다른 외부 AS 간에 **경로 정보**를 교환하여 패킷이 전 세계를 건너갈 수 있게 합니다.
- 주요 프로토콜로는 **BGP(Border Gateway Protocol)** 가 사용되며 단순히 물리적 거리뿐만 아니라 사업자 간의 비용, 보안 정책, 트래픽 협약 등에 기반하여 **최적의 경로**를 선택합니다.

# 9. HTTP 응답 메시지

> 패킷이 구글 웹 서버에 도달하고 클라이언트 요청에 대한 결과물을 `HTTP 응답 메시지`로 생성하는 단계입니다.

## 9.1 HTTP 응답 메시지 생성

```text
Status Line: 'HTTP/1.1 200 OK'
Response Headers: 'Content-Type, Date, Server'
Message Body: '구글 메인 페이지의 HTML 코드'
```

- 웹 서버는 처리가 완료되면 **HTTP 버전**, **상태 코드**, **헤더**, **실제 데이터**를 포함한 응답 메시지를 생성합니다.

## 9.2 HTTP 상태 코드

```text
2xx(Success): '요청이 정상적으로 처리됨'
3xx(Redirection): '리소스의 경로 변경으로 인해 재요청 필요'
4xx(Client Error): '클라이언트 요청 오류'
5xx(Server Error): '서버 내부 오류'
```

- HTTP 상태코드는 클라이언트의 요청에 대해 서버가 **처리 결과**를 **숫자**로 알려주는 응답 정보입니다.

## 9.3 HTTP 응답 메시지 전송

```text
HTTP Response → TCP(Segment) → IP(Packet) → Ethernet(Frame)
```

- 생성된 HTTP 응답 메시지는 캡슐화 과정을 거치고 호스트의 **공인 IP 주소**로 전송됩니다.
  
# 10. HTTP 응답 메시지 수신

> 전송받은 HTTP 응답 메시지는 `역캡슐화` 과정을 거치고 브라우저 엔진에 의해 웹 페이지로 렌더링됩니다. 

## 10.1 NAT 역변환

```text
Public IP(Destination) → Default Gateway → Private IP(Host)
```

- 호스트의 공인 IP를 달고 도착한 패킷은 기본 게이트웨이 NAT 테이블을 참조하여 호스트의 **사설 IP**와 **포트 번호**로 다시 변환됩니다.

## 10.2 역캡슐화

```text
NIC → Ethernet(Frame) → IP(Packet) → TCP(Segment) → HTTP(Data)
```

- 호스트의 NIC부터 시작하여 Ethernet, IP, TCP 헤더를 차례로 **제거**하며 데이터를 상위 계층으로 전달합니다.

## 10.3 브라우저 렌더링

- HTML 데이터를 바탕으로 브라우저 엔진이 화면을 **렌더링**하면 구글 메인 페이지가 출력이 됩니다.