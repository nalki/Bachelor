# 네트워크 통신의 과정

---

# 1. URL 입력 및 DNS 조회

> 클라이언트가 브라우저 주소창에 `www.google.com`을 입력하면 시스템은 이 문자를 컴퓨터가 이해할 수 있는 IP 주소로 변환하기 위해 DNS 서비스를 이용합니다.

## 1.1 Cache 확인 및 DNS 서버 접속

```text
Browser Cache: '내부 IP 주소 확인'
Client → DNS: '서버 접속'
```

- 먼저 PC 내부 Cache의 기록을 확인하여 기록이 없다면 DNS 서버와 통신을 시작합니다. 
- DNS는 오버헤드가 적고 빠른 응답이 가능한 **UDP 프로토콜(포트 53)** 를 사용합니다.

## 1.2 DNS 질의 수행

### 1.2.1 재귀적 질의

```text
Client → DNS: '최종 IP 주소 요청'
```

- 클라이언트는 DNS 서버에게 최종 IP 주소를 요청합니다.

### 1.2.2 반복적 질의

```text
① DNS → Root DNS: '.com 네임 서버 주소 반환'
② DNS → TLD DNS: 'google.com 네임 서버 주소 반환'
③ DNS → Authoritative DNS: 'www.google.com의 최종 IP 주소 반환'
```

- DNS 서버가 전 세계 계층화된 하위 DNS 서버를 하나씩 접속해 정보를 수집하는 과정입니다. 
- 각 서버는 자신이 아는 범위 내에서 다음 목적지를 알려줍니다.

## 1.3 IP 주소 반환

```text
"DNS → Client: '최종 IP 주소 반환'
```

- 모든 질의를 마친 DNS 서버가 최종 IP 주소를 클라이언트에게 반환합니다.

# 2. TCP 연결

> 데이터의 안전한 전송을 위해 `TCP 프로토콜(포트 80)`을 사용하여 서버와 신뢰성 있는 연결을 맺습니다.

## 2.1 세션 수립(3-Way-Handshake)

```text
① Client → Server: 'SYN(연결 요청)'
② Server → Client: 'SYN-ACK(연결 요청 확인 및 응답)'
③ Client → Server: 'ACK(연결 확립 완료)'
```

- **HTTP 프로토콜(포트 80)** 메시지 요청을 위해 3번의 패킷 교환을 통해 두 장치 간의 논리적인 통신 통로가 완전히 개방됩니다.

# 3. HTTP 요청

> 연결된 통로를 통해 사용자가 원하는 웹 페이지를 가져오기 위한 요청 메시지를 작성하는 단계입니다.

## 3.1 HTTP 요청 메시지 생성

```http
GET / HTTP/1.1
Host: [www.google.com](https://www.google.com)
User-Agent: Mozilla/5.0 (Windows NT 10.0; Win64; x64) ...
Accept: text/html,application/xhtml+xml ...
Connection: keep-alive
```

- 브라우저는 구글 서버에게 메인 화면 데이터를 요청하는 GET 메시지를 생성합니다. 
- 생성된 HTTP 요청 메시지는 크게 세 부분(시작 줄, 헤더, 바디)으로 나누어집니다.

## 3.2 HTTP 요청 메시지 구조

### 3.2.1 시작 줄

```text
GET / HTTP/1.1
메서드(GET) + 요청 경로(/) + 프로토콜 버전(HTTP/1.1)
```

- 서버에 무엇을(GET), 어디서(/), 어떤 방식으로(HTTP/1.1) 처리해달라고 명시하는 첫 번째 줄입니다.

### 3.2.2 헤더

```text
Host: '요청이 전송될 서버의 도메인 주소'
User-Agent: '요청을 보내는 브라우저 및 OS의 정보'
Accept: '브라우저가 이해할 수 있는 콘텐츠 타입(HTML, XML 등)'
Connection: '통신 연결 유지 여부(keep-alive)'
```

- 헤더는 메시지 자체에 대한 부가 정보를 담고 있습니다. 
- 서버는 이 헤더 정보를 보고 브라우저의 환경에 최적화된 응답을 준비합니다.

### 3.2.4 바디

```text
GET 요청의 경우 보통 비어 있음
```
- 서버로 보낼 실제 데이터가 담기는 부분입니다. 현재는 단순히 페이지를 조회하는 **GET** 방식이므로 데이터 없이 전송됩니다.
- POST 방식의 경우 로그인 정보나 게시글 내용이 담깁니다.

### 3.2.5 메소드 

```text
GET(조회): '서버로부터 특정 리소스를 요청'
POST(생성): '서버에 새로운 리소스를 생성'
PUT(수정): '서버의 리소스를 요청 메시지의 내용으로 대체'
PATCH(부분 수정): '리소스의 일부분을 수정'
DELETE(삭제): '서버의 특정 리소스를 삭제'
```

- HTTP 메소드는 클라이언트가 서버에게 요청의 목적을 알리는 수단입니다.

# 4. 전송 계층

> 응용 계층에서 완성된 HTTP 요청 메시지가 하위 계층인 전송 계층으로 전달되어, 데이터의 전송 방식과 목적지 프로세스를 결정하는 단계입니다.

## 4.1 세그먼트 생성

```text
HTTP 요청 메시지 + TCP Header → TCP Segment
```

- 전송 계층은 전달받은 HTTP 메시지 데이터에 전송 제어 정보를 담은 **TCP 헤더**를 추가해 캡슐화하며 이 단위를 **세그먼트(Segment)**라고 부릅니다.

## 4.2 TCP 헤더 구조

```text
Source Port: '클라이언트에서 임의로 할당한 포트 번호'
Destination Port: '목적지 서버의 서비스 포트 번호'
Sequence Number: '분할된 세그먼트의 순서를 나타내는 번호'
Acknowledgment Number: '데이터를 잘 받았음을 확인해주는 응답 번호'
Window Size: '한 번에 보낼 수 있는 데이터의 양'
Checksum: '데이터 전송 중 오류 발생 여부를 확인하기 위한 값'
```

- 여기서 가장 핵심은 **Destination Port** 입니다. 구글 서버에 도착했을 때 수많은 프로그램 중 '웹 서버(HTTP)'를 찾아가게 만드는 이정표 역할을 합니다.

# 5. 네트워크 계층

> 전송 계층에서 내려온 세그먼트에 논리적 주소인 IP 정보를 부여하여, 전 세계 네트워크 망에서 목적지를 찾아갈 수 있도록 패킷화하는 단계입니다.

## 5.1 패킷 생성

```text
TCP Segment + IP Header → IP Packet
```

- 네트워크 계층은 전송 계층에서 전달 받은 세그먼트를 페이로드로 삼고 **IP 헤더**를 추가해 캡슐화하며 이 데이터 단위를 **패킷(Packet)** 이라고 부릅니다.

## 5.2 IP 헤더 구조

```text
Version: 'IP 버전 정보'
Time To Live(TTL): '패킷이 네트워크상에 살아있을 수 있는 시간'
Protocol: '상위 계층 프로토콜 타입'
Source IP Address: '송신자의 공인 IP 주소'
Destination IP Address: '수신자의 공인 IP 주소'
```

- 전송 계층의 포트 번호가 어떤 프로그램인지를 알려준다면 IP 주소는 어떤 집인지를 알려주는 논리적 주소 역할을 합니다.

## 5.3 라우팅 수행

- Google의 공인 IP 주소는 다른 서브넷이므로 라우팅 테이블에 설정된 기본 게이트웨이 규칙에 따라 기본 게이트웨이 IP 주소로 패킷을 전송합니다.

## 5.4 ARP 프로토콜 수행

### 5.4.1 ARP Cache 확인

- PC는 먼저 자신의 메모리에 있는 ARP 테이블을 확인합니다.

### 5.4.2 ARP Request

- PC의 ARP 테이블에 기본 게이트웨이의 MAC 주소가 없으면 자신의 페이로드에 기본 게이트웨이의 IP 주소를 담아 브로드캐스트 방식 즉 네트워크 안의 모든 노드에 요청 패킷을 전송합니다.

### 5.4.3 ARP Reply

- 요청 패킷을 받은 기본 게이트웨이는 자신의 페이로드에 MAC 주소를 담아 유니캐스트 방식 즉 요청한 PC에게만 패킷을 전송합니다.

### 5.4.4 결과 반영 및 전송 준비

- PC는 전송 받은 MAC 주소를 ARP 테이블에 업데이트하고, 데이터를 하위 계층인 데이터링크 계층으로 전달합니다.

> **코멘트:** 이 과정을 통해 '논리적 주소(IP)'와 '물리적 주소(MAC)'의 결합이 완료됩니다. 이제 데이터는 진짜 물리적인 형태인 **프레임(Frame)**으로 변환될 준비를 마쳤습니다.

## 6. 데이터 링크 계층

> 네트워크 계층에서 내려온 패킷에 MAC 주소 정보를 추가하여 인접한 네트워크 장비 간의 신뢰성 있는 전송을 보장하는 프레임(Frame)을 생성하는 단계입니다.

### 6.1 프레임 생성
```text
Ethernet Header + IP Packet + Ethernet Trailer → Ethernet Frame
```
- 데이터 링크 계층은 패킷을 페이로드로 삼아 앞에는 **헤더(Header)**, 뒤에는 오류 검출을 위한 **트레일러(Trailer)** 를 추가해 캡슐화하며 이 단위를 **프레임(Frame)** 이라고 부릅니다.



### 6.2 이더넷 헤더 구조
```text
Destination MAC Address: '다음 목적지의 물리적 주소'
Source MAC Address: '송신자의 물리적 주소'
EtherType: '상위 계층의 프로토콜 타입'
FCS(Frame Check Sequence): '데이터 에러 발생 여부를 위한 체크섬 값'
```

- 이더넷은 오직 **인접한 노드** 즉 같은 스위치에 연결된 장비의 통신만을 책임집니다.
- MAC 주소는 홉(hop)마다 계속 변경됩니다.

### 6.3 인접 노드 간의 물리적 전송 준비
* **내용:** 프레임 작성이 완료되면, 이제 이 데이터를 전기적 신호로 변환하여 실제 케이블로 내보낼 준비를 마칩니다.
> **코멘트:** L2 단계가 끝나면 소프트웨어적인 포장은 모두 끝난 셈입니다. 이제 이 '0'과 '1'의 덩어리는 물리 계층을 통해 실제 전선이나 무선 신호로 변환됩니다.

## 7. 물리 계층
데이터 링크 계층에서 완성된 프레임(비트 단위)을 실제 전송 매체를 통해 흐를 수 있는 물리적 신호(전기, 빛, 전파)로 변환하여 전송하는 단계입니다.

## 7.1 비트 스트림의 신호 변환
* **과정:** Digital Bits (0, 1) → Physical Signals
* **변환 방식:** * **전기 신호:** 구리선(UTP 케이블)을 통해 전압의 차이로 데이터 전송
    * **광 신호:** 광섬유(Fiber Optic)를 통해 빛의 점멸로 데이터 전송
    * **무선 신호:** 전자기파(Radio Waves)를 통해 특정 주파수로 데이터 전송
> **코멘트:** 컴퓨터 내부의 디지털 데이터는 그 자체로 전선을 타고 나갈 수 없습니다. 따라서 NIC(랜카드)는 이 '0'과 '1'의 조합을 전기적 파동이나 빛의 신호로 바꾸어주는 '번역가' 역할을 수행합니다.

## 7.2 물리적 전송 매체 및 하드웨어
데이터가 이동하는 실제 통로와 그 과정에서 신호를 증폭하거나 전달하는 장비들입니다.

* **NIC (Network Interface Card):** 디지털 데이터를 물리적 신호로 변환하여 케이블로 내보내는 장치
* **케이블 (Transmission Media):** UTP(랜선), 광케이블 등 실제 데이터가 흐르는 매체
* **허브/리피터 (Hub/Repeater):** 감쇄된 신호를 재생하거나 여러 경로로 전달하는 물리 계층 장비
> **코멘트:** 이 단계에서는 데이터의 내용(IP나 포트)을 전혀 보지 않습니다. 오직 들어온 신호를 그대로 증폭하거나 다른 포트로 전달하는 물리적 작업만 수행합니다.

## 7.3 데이터의 방출
* **내용:** 변환된 신호가 내 PC의 랜 포트를 떠나 랜선(UTP)을 타고 공유기나 벽면 단자함으로 향합니다.
> **코멘트:** 이제 데이터는 드디어 내 컴퓨터라는 물리적 경계를 벗어나 거대한 네트워크 바다로 첫 발을 내디뎠습니다.

